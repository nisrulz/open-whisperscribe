audio:
  sample_rate: 16000
  audio_file: output.wav

hotkey:
  debounce_time_ms: 1000  # milliseconds

whisper:
  model: tiny  # Possible values: tiny, base, small, medium, large

logger:
  console: true
